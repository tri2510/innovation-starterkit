# ============================================
# REQUIRED: API Keys
# ============================================
# Only these API keys are required to run the application
# All other settings have sensible defaults built into the code

# z.ai API Key (Required)
# Get your API key from: https://z.ai
OPENAI_API_KEY=your_openai_api_key_here

# Tavily API Key (Optional - for web search features)
# Get your free API key from: https://tavily.com (1,000 requests/month free)
# If not set, web search features will be disabled
TAVILY_API_KEY=your_tavily_api_key_here

# ============================================
# OPTIONAL: Override Defaults
# ============================================
# The following settings have built-in defaults.
# Only uncomment if you need to customize them.

# z.ai API Base URL (Default: https://api.z.ai/api/anthropic)
# For Anthropic SDK compatibility
# OPENAI_BASE_URL=https://api.z.ai/api/anthropic

# Default AI Model (Default: glm-4.7)
# OPENAI_DEFAULT_MODEL=glm-4.7

# API Timeout in milliseconds (Default: 60000)
# API_TIMEOUT=60000

# API Retry Attempts (Default: 3)
# API_MAX_RETRIES=3

# Demo Mode (Default: false)
# Set to 'true' to enable demo mode with sample data
# NEXT_PUBLIC_DEMO_MODE=true

# Disable Onboarding Tour (Default: false)
# Set to 'true' to hide the onboarding tour
# NEXT_PUBLIC_DISABLE_TOUR=true
